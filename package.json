{
  "scripts": {
    "lint": "awesome-lint",
    "build": "markdown-toc -i --bullets=- --skip=License --maxdepth=3 README.md",
    "test": "npm run build -- --check && npm run lint"
  },
  "devDependencies": {
    "awesome-lint": "^0.11.0",
    "@csabapalfi/markdown-toc": "2.0.0"
  }
}
